<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.happyjob.study.sAlert.dao.SNoticeDao">

<!-- 공지사항 목록 조회 -->
<select id="sListNotice" resultType="kr.happyjob.study.sAlert.model.SNoticeModel">
	SELECT 
	    ui.user_type,
	    n.loginID,
	    n.notice_no,
	    n.file_no,
	    n.notice_title,
	    n.notice_content,
	    DATE_FORMAT(n.notice_created_at , '%Y-%m-%d') as notice_created_at
	FROM tb_notice n
	LEFT OUTER JOIN 
	    tb_userinfo ui ON ui.loginID = n.loginID
	ORDER BY n.notice_created_at DESC;
</select>


</mapper>